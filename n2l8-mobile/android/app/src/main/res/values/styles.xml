<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied when the app launches (splash phase) -->
    <style name="LaunchTheme" parent="android:Theme.Black.NoTitleBar">
        <item android:windowBackground="?android:colorBackground" />
    </style>
    
    <!-- Theme applied after the launch sequence finishes -->
    <style name="NormalTheme" parent="android:Theme.Black.NoTitleBar">
        <item android:windowBackground="?android:colorBackground" />
    </style>
</resources>
